version: 0.2
phases:
  build:
    commands:
      - npm install
      - aws codeartifact login --tool npm --repository serverless --domain quotable-value --domain-owner 948396734470 --region ap-southeast-2
      - npm publish --registry https://quotable-value-948396734470.d.codeartifact.ap-southeast-2.amazonaws.com/npm/serverless/
